import { FilterDevicePipe } from './filter-device.pipe';

describe('FilterDevicePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDevicePipe();
    expect(pipe).toBeTruthy();
  });
});
